import React from 'react'
import { Alert } from 'react-bootstrap'

function ErrorComponent() {
    return (
        <Alert variant="warning">Errore - Stai utilizzando il tuo Bearer token?</Alert>
    )
}

export default ErrorComponent
